# Ticket 55 — Notes

- `LOGIN_GRID_TOP_DARK` = `auraPalette.darkBg` để `useAuthGridChrome` / system root khớp theme.
- Glass dark: tránh `rgba(0,0,0,0.4)` — trùng với surface Aura `#261C20` có alpha.
