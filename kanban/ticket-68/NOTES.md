# Ticket 68 — Notes

- Postgres: chạy migrate sau `015` để có `show_online_status`.
- Client cũ / session cache thiếu field: coi `showOnlineStatus === false` mới là tắt; `undefined` → bật (mặc định sản phẩm).
