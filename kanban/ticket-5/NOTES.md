# Ticket 5 — notes

- **`RegisterScreenShell`** đổi tên hợp nhất → **`AuthGridScreenShell`** để register + forgot-password dùng chung.
- **Không dùng `React.memo`** trên section (bài học ticket hotfix Fabric + React 19).
