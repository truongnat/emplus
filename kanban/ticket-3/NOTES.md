# Ticket 3 — notes

- **Giới tính**: UI thêm chip `KHONG_TIET_LO` (“Riêng tư”) cho khớp `RegisterSchema`.
- **Hero**: Tái dùng `LoginHeroSection` (Lottie mèo) cho đồng bộ cảm xúc với login.
- **Session sau đăng ký**: `useRegister` vẫn `setSession(data)` như cũ; điều hướng OTP giữ nguyên.
