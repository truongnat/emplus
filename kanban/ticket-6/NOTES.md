# Ticket 6 — notes

- **`AUTH_GRID_SCROLL_PADDING_EXTRA`** = 2 + 40 + 2 = **44** (đồng bộ với hàng back 40px + offset + gap).
- Login không có back chip nhưng dùng **cùng** `paddingTop` scroll để hero/form thẳng hàng với register/forgot.
