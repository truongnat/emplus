# Ticket 36 — Notes

## Kỹ thuật

- Chiều rộng trang carousel = `onLayout` của `imageSection` (trừ axis, card đã `flex: 1`).
- `getItemLayout` cố định theo `stripW` để paging ổn định.
- `keyExtractor` cắt URI để tránh key quá dài.

## Skills (tham chiếu workflow)

- `react-native-pro` / `mobile-design`: touch target, nested scroll.
- Không thêm skill mới trong repo.
