import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function TimelinePage() {
  return (
    <PlaceholderPage
      eyebrow="Memories"
      title="Timeline placeholder."
      description="Timeline memories are not wired yet. This route only anchors the future memory experience."
      primaryHref="/poke"
      primaryLabel="Preview poke"
    />
  );
}
