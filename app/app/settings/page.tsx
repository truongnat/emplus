import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Settings"
      title="Settings placeholder."
      description="Account, couple profile, install state, and notification preferences will be added in later phases."
      primaryHref="/"
      primaryLabel="Back to start"
    />
  );
}
