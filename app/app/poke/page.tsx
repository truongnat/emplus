import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function PokePage() {
  return (
    <PlaceholderPage
      eyebrow="Poke"
      title="Poke placeholder."
      description="The lightweight partner nudge surface will be added later without WebSockets in phase one."
      primaryHref="/gifts"
      primaryLabel="Preview gifts"
    />
  );
}
