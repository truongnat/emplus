import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function RegisterPage() {
  return (
    <PlaceholderPage
      eyebrow="Access"
      title="Register placeholder."
      description="Account creation is intentionally not wired yet. This route only reserves the PWA registration surface."
      primaryHref="/setup"
      primaryLabel="Preview setup"
    />
  );
}
