import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SetupPage() {
  return (
    <PlaceholderPage
      eyebrow="Couple setup"
      title="Setup placeholder."
      description="Couple profile and love start date setup will be implemented after the scaffold is stable."
      primaryHref="/invite"
      primaryLabel="Preview invite"
    />
  );
}
