import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Access"
      title="Login placeholder."
      description="Authentication UI will be added later without changing the current backend token/session flow."
      primaryHref="/home"
      primaryLabel="Preview home"
    />
  );
}
