import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function IndexPage() {
  return (
    <PlaceholderPage
      eyebrow="Em+"
      title="A quiet place for two."
      description="The PWA workspace is ready for the MVP shell. Product features, auth, API calls, and PWA service worker work come later."
      primaryHref="/login"
      primaryLabel="Continue"
    />
  );
}
