import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function GiftsPage() {
  return (
    <PlaceholderPage
      eyebrow="Gifts"
      title="Gift suggestions placeholder."
      description="Gift suggestions will come from backend-owned static config. No commerce scraping is included."
      primaryHref="/settings"
      primaryLabel="Preview settings"
    />
  );
}
