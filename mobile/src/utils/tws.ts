/**
 * Tailwind passthrough utility for compatibility.
 * In Tamagui architecture, this is mostly redundant.
 */
export function tws(classes: any, ...args: any[]): any {
  return {}; // Return empty object since we are moving away from nativewind/manual styles
}
